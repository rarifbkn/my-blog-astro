---
import BaseLayout from "../layouts/BaseLayout.astro";
const pageTitle = "About";

const identity = {
  firstName: "Arif",
  country: "Chile",
  occupation: "Software engineering",
  hobbies: ["gamer", "voleyball", "soccer"],
};

const skills = ["HTML", "CSS", "Javascript", "React", "Astro", "Writing Docs"];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeigh = "bold";
const textCase = "uppercase";
---

<BaseLayout>
  <h1>{pageTitle}</h1>
  <h2>... and new Astro site!</h2>
  <p>
    I am working trough Astro's introductory tutorial. This is the second page
    on my website, and it's the first one I built myself
  </p>
  <p>
    This site will update as I complete more of the tutorial, so keep checking
    back and see how my journey is going!
  </p>

  <p>Here a few facts about me:</p>
  <ul>
    <li>My name is {identity.firstName}.</li>
    <li>
      I live in {identity.country} and I work as a {identity.occupation}.
    </li>
  </ul>
  <p>My skills are:</p>
  <ul>
    {skills.map((skill) => <li class="skill">{skill}</li>)}
  </ul>
  {happy && <p>I am happy to use to be learning Astro!</p>}
  {finished && <p>I finished this tutorial!</p>}
  {
    goal === 3 ? (
      <p>My goal is to finish in 3 days.</p>
    ) : (
      <p>My goal is not 3 days.</p>
    )
  }
  <style define:vars={{ skillColor, textCase, fontWeigh }}>
    :root {
      --skill-color: "navy";
      --font-weight: "bold";
      --text-case: "uppercase";
    }
    .skill {
      color: var(--skillColor);
      font-weight: var(--fontWeigh);
      text-transform: var(--textCase);
    }
  </style>
</BaseLayout>
